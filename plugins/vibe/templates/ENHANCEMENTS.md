<!-- guidance: created by /vibe:wrap-session the first time something is cut or deferred — not
     at kickoff, because a file of ideas before there is a thing teaches the wrong lesson. When
     creating it, say in one sentence why it exists; the line under the title is the script.
     Strip guidance comments; keep plain ones. -->

# Enhancements — {{project-name}}

This is where the version you actually wanted lives while Claude builds the version that ships.

<!-- Nothing is lost here, and that is the whole function. Claude will always build the fastest
     path to something working, which means real parts of the idea get set aside along the way.
     Written down, they become the next phase. Unwritten, they get rediscovered months later as
     a disappointment. -->

## Next up

{{next-items}}

<!-- guidance: the two or three things that genuinely come next. This is what
     /vibe:start-session offers first when a phase finishes, so keep it short and keep it
     honest — a priority list with nine items on it is not a priority list. -->

## The menu

{{menu}}

<!-- guidance: everything banked. Group by theme once there is enough to group; grouping is what
     keeps this readable at thirty items. Each entry: what it is, why it matters, and a rough
     size (S / M / L). Add the date when it matters — six months on, when an idea arrived is
     often the context that decides. Entries land the moment something is cut, not at the end
     of the session.

     Shape:
     ### Theme
     - **The thing** — why it matters, in a clause or two. (M, deferred YYYY-MM-DD) -->

## Known gaps

{{gaps}}

<!-- guidance: things that are missing rather than wanted — a rough edge shipped knowingly, a
     case not handled, a manual step nobody has automated yet. Distinct from the menu because
     these are not ideas for later; they are the honest state of what exists. Mark an entry ✅
     when it closes rather than deleting it, so the record shows the project getting better. -->

## Shipped

{{shipped}}

<!-- guidance: entries move here when a phase delivers them — moved, not deleted, with the date
     and the phase that did it. This is the record of the project growing. -->

<!-- One boundary worth holding: operational knowledge does not belong in this file. How to run
     a migration safely, how development and production differ, what to never do to the
     database — that belongs in ARCHITECTURE.md's decisions or CLAUDE.md's house rules, where a
     session will actually read it before acting. This file is only for what to build next; ops
     notes buried in a wish list get found after the mistake. -->
