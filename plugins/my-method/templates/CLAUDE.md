<!-- guidance: instantiated by establish. Nothing to fill — copy it as-is, stripping this
     comment. The "@AGENTS.md" line is a Claude Code import; other tools ignore it and read
     the sentence.

     If a CLAUDE.md already exists with real content, do not replace it: move its content into
     AGENTS.md (house rules, local-only ground, session log) and leave this pointer behind.
     If a tool-managed block exists in either file — framework scaffolders write these, marked
     with BEGIN/END comments — keep it, add the pointer text above it, and note that it is
     tool-managed. -->

# Agent instructions

@AGENTS.md

Read `AGENTS.md` in this folder. It is the single set of instructions for this project, whatever tool you are, and it is the only one kept current.

This file stays a pointer on purpose. Keeping two instruction files in step is a job nobody does reliably, and the failure is silent — the stale one gets followed.
